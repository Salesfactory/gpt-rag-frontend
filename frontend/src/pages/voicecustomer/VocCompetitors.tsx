import React from "react";
import styles from "./VocPlaceholder.module.css";

const VocCompetitors: React.FC = () => {
  return (
    <section className={styles.section}>
      <h1 className={styles.title}>Competitors</h1>
      <p className={styles.text}>Placeholder page for managing competitors.</p>
    </section>
  );
};

export default VocCompetitors;
